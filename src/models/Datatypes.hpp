#ifndef DATATYPES_HPP
#define DATATYPES_HPP

#include <vector>
#include <QPixmap>

/* Defining the Image type */
using Image = QPixmap;

/* Defining the Comic type */
using Comic = std::vector<QPixmap>;


#endif